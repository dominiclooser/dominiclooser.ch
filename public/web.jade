extends _default

block content
   h1.page-heading Web
   .intro
      p.
         Ich gestalte und entwickle Webseiten: individuell, reduziert und ästhetisch.
   #sites
      for site in sites
         figure.site
            a(href=site.link)
               img(src='/images/sites/' + site.image)
            figcaption
               h2= site.title
               p= site.category